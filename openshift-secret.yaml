apiVersion: v1
kind: Secret
metadata:
  name: idea-hub-secret
  namespace: 
type: Opaque
stringData:
  # Database credentials (sensitive)
  PG_USER: 
  PG_PASS: 
  PG_DB: 
  PG_HOST: 
  PG_PORT: "2081"
  
  # AI API credentials (sensitive)
  GEMINI_API_KEY: 
  
  # Note: All other configuration is now static in config/settings.py
  # This includes: DEBUG, SECRET_KEY, GEMINI_MODEL, EMBEDDING_MODEL, etc.
